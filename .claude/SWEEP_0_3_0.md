# phobic 0.3.0 benchmark sweep

Generated by .claude/sweep_0_3_0.py on a 12-core machine.
Build is `phobic.build(...)` with default load_factor=0.5; 
auto num_shards. All keys are 16 random bytes (cipher-maps shape).
Median build time across reps; query is the C batch path. 
Wall-clock numbers are illustrative; absolute values vary by CPU.

## Headline

| N | num_shards | bpk | build @ 1 thread | build @ 12 threads | parallel speedup |
|---:|---:|---:|---:|---:|---:|
| 1K | 1 | 2.37 | <1 ms | <1 ms | (tiny) |
| 100K | 7 | 1.17 | 57 ms | 26 ms | 2.2x |
| 1M | 63 | 1.17 | 636 ms | 306 ms | 2.1x |
| 10M | 625 | 1.16 | 7.42 s | 3.72 s | 2.0x |

### Comparison points

vs phobic 0.2.0 (from `.claude/SESSION_NOTES.md`, 8 cores):
- 1M parallel: 2.0 s in 0.2.0; **0.34 s in 0.3.0** (5.9x faster), bpk 1.18 to 1.17 (slightly better).

vs maph's reference `partitioned_phf<phobic4>` (from maph's BENCHMARK_RESULTS.md):
- 10M build: 7.4 s in maph; **3.72 s in 0.3.0** (2.0x faster), bpk 2.76 to 1.16 (2.4x more space-efficient).

vs maph's `shock_hash128` (the space champion in maph):
- 10M bpk: 1.54 in 14.3 s with range/keys=1.85; phobic 0.3.0 hits 1.16 in 3.72 s and is minimal. Phobic wins on every axis at this N.

## Auto num_shards heuristic

`num_shards` is now a pure function of N (independent of `num_threads`),
so `phobic.build(keys, seed=42)` produces byte-identical output regardless
of CPU count. Below 32K keys: 1 shard. Above: `(N + 15999) / 16000`.

## Notes on the speedup ceiling (and what 0.3.1 fixed)

Initial 0.3.0 parallel build speedup was ~2x even at 12 threads.
Phase-level profiling (gated on `-DPHOBIC_PROFILE`, results in
`.claude/PROFILE_0_3_0.md` if rerun) revealed that the C parallel
section was already fast (520 ms at 12 threads on 10M keys) and the
real cost was in the Python preamble before any C work began:

- `[k.encode() if str else bytes(k) for k in keys]`: 1.9 s at 10M.
  The `bytes(k)` call always copies, even when `k` is already bytes.
- `len(set(raw)) != len(raw)` uniqueness check: 2.1 s at 10M. Hashes
  every key into a Python set; for callers that already know their
  keys are unique (cipher-maps, where keys are HMAC outputs), this
  is pure waste.
- A flat-buffer memcpy in `py_build` (160 ms at 10M): defensive copy
  of every byte before releasing the GIL. Unnecessary because PyBytes
  is immutable and the keys list reference is held throughout.

phobic 0.3.1 fixes all three:

| Config | 0.3.0 | 0.3.1 (default) | 0.3.1 (assume_unique=True) |
|---|---:|---:|---:|
| 1 thread | 7762 ms | 6098 ms | 4295 ms |
| 4 threads | 4230 ms | 2789 ms | 1588 ms |
| 8 threads | 3822 ms | 2342 ms | 1200 ms |
| 12 threads | 3712 ms | 2369 ms | **1181 ms** |

**0.3.0 -> 0.3.1 at 12 threads, with `assume_unique=True`: 3.14x faster.**

Parallel scaling within 0.3.1 itself improved as well: from 2x (0.3.0)
to 2.6x (0.3.1 default) and 3.6x (0.3.1 with `assume_unique=True`),
because Python-side serial work is no longer the binding constraint.

What's left for 0.4.0:

- Per-thread arena allocator (malloc contention across shard scratch buffers)
- Parallel shard distribution pass (currently 50 ms single-threaded)
- numpy bulk-input/bulk-output for batch query (would skip per-key
  PyBytes/PyLong allocations, possibly 4-5x batch query speedup)
- SIMD inner loop in `slot_with_pilot` (4-8 keys/cycle with AVX2/AVX-512)

## Batch query summary

| N | sample size | 1 thread (ns/key) | 8 threads (ns/key) |
|---:|---:|---:|---:|
| 1K | 1K | 142 | 141 |
| 100K | 10K | 149 | 166 |
| 1M | 50K | 175 | 151 |
| 10M | 50K | 211 | 174 |

Batch parallelism is small (~1.2x at most) because the work per key
(~150 ns of C: one wymix, one bucket lookup, one slot mod) is small
relative to per-key Python overhead (PyBytes encoding under GIL,
PyLong_FromSize_t per output). A numpy bulk-input + bulk-output path
would shift this dramatically (4-5x feasible) but is a 0.4.0 feature.

---

## Raw data

## N = 1,000

### build (median of 5 reps; bpk and num_shards from one)

| num_threads | build (s) | bpk  | num_shards |
|------------:|----------:|-----:|-----------:|
|           1 |     0.000 | 2.37 |          1 |
|           4 |     0.000 | 2.37 |          1 |
|           8 |     0.000 | 2.37 |          1 |
|          12 |     0.000 | 2.37 |          1 |

### batch query (1,000 keys, median of 5 reps)

| num_threads | ns/key |
|------------:|-------:|
|           1 |  141.6 |
|           8 |  140.9 |

## N = 100,000

### build (median of 3 reps; bpk and num_shards from one)

| num_threads | build (s) | bpk  | num_shards |
|------------:|----------:|-----:|-----------:|
|           1 |     0.057 | 1.17 |          7 |
|           4 |     0.029 | 1.17 |          7 |
|           8 |     0.025 | 1.17 |          7 |
|          12 |     0.026 | 1.17 |          7 |

### batch query (10,000 keys, median of 5 reps)

| num_threads | ns/key |
|------------:|-------:|
|           1 |  149.3 |
|           8 |  166.1 |

## N = 1,000,000

### build (median of 3 reps; bpk and num_shards from one)

| num_threads | build (s) | bpk  | num_shards |
|------------:|----------:|-----:|-----------:|
|           1 |     0.636 | 1.17 |         63 |
|           4 |     0.372 | 1.17 |         63 |
|           8 |     0.340 | 1.17 |         63 |
|          12 |     0.306 | 1.17 |         63 |

### batch query (50,000 keys, median of 5 reps)

| num_threads | ns/key |
|------------:|-------:|
|           1 |  174.5 |
|           8 |  151.4 |

## N = 10,000,000

### build (median of 1 reps; bpk and num_shards from one)

| num_threads | build (s) | bpk  | num_shards |
|------------:|----------:|-----:|-----------:|
|           1 |     7.416 | 1.16 |        625 |
|           4 |     4.193 | 1.16 |        625 |
|           8 |     3.834 | 1.16 |        625 |
|          12 |     3.722 | 1.16 |        625 |

### batch query (50,000 keys, median of 5 reps)

| num_threads | ns/key |
|------------:|-------:|
|           1 |  211.2 |
|           8 |  173.7 |
